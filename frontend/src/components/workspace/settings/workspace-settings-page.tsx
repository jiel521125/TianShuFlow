"use client";

import { WorkspaceManager } from "@/components/workspace/workspace-manager";

/** Settings-tab wrapper around the shared workspace manager. */
export function WorkspaceSettingsPage() {
  return <WorkspaceManager />;
}
