{{- if not .Values.existingAppSecret -}}
{{- $prev := lookup "v1" "Secret" (include "tian-shu.namespace" .) (include "tian-shu.appSecret" .) -}}
{{- $betterAuth := "" -}}
{{- $internalToken := "" -}}
{{- if $prev -}}
{{- $betterAuth = index $prev.data "BETTER_AUTH_SECRET" | default "" | b64dec -}}
{{- $internalToken = index $prev.data "TIAN_SHU_INTERNAL_AUTH_TOKEN" | default "" | b64dec -}}
{{- end -}}
{{- if not $betterAuth -}}{{- $betterAuth = randAlphaNum 48 | lower -}}{{- end -}}
{{- if not $internalToken -}}{{- $internalToken = randAlphaNum 40 -}}{{- end -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "tian-shu.appSecret" . }}
  namespace: {{ include "tian-shu.namespace" . }}
  labels:
    {{- include "tian-shu.labels" . | nindent 4 }}
type: Opaque
stringData:
  BETTER_AUTH_SECRET: {{ $betterAuth | quote }}
  TIAN_SHU_INTERNAL_AUTH_TOKEN: {{ $internalToken | quote }}
{{- end -}}
