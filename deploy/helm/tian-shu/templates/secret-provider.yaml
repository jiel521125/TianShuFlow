{{- if and (not .Values.existingSecret) .Values.secrets -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "tian-shu.providerSecret" . }}
  namespace: {{ include "tian-shu.namespace" . }}
  labels:
    {{- include "tian-shu.labels" . | nindent 4 }}
type: Opaque
stringData:
{{- range $k, $v := .Values.secrets }}
  {{ $k }}: {{ $v | quote }}
{{- end }}
{{- end -}}
